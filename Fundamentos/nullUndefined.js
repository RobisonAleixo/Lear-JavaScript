let valor;  // Declarado porém não inicializado => undefined
console.log(valor);

console.log(valor);     // Nem declarado nem inicializado => ERRO

valor = null;   // Não aponto para nenhum local de memória.